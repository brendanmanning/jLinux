
/**
 * Updates every 3rd party app from the appstore by getting the newest version
 * 
 * @author (Brendan Manning) 
 * @version (jLinux 2.5)
 */

import java.util.*;
public class updateapps
{
   public static boolean doUpdate() {
       System.out.println("This command is not finished yet!");
       System.out.println("Future versions of jLinux wil have a working updateapps command");
       /*
        * List allInstalled = listapps.list();
       return true;
       for (int i = 0; i < allInstalled.size(); i++) {
			main.deleteContents(allInstalled.get(i));
		}
        */
       return true;
   }
}
